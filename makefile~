CC = g++
OFLAG = -o
CFLAGS = -c -Wall -g -DDEBUG
LIB = -lGLEW -lglfw -lGL

all: main.o
	$(CC) $(OFLAG) main main.o  $(LIB_DIR) $(LIB) $(WIN_GUI_FLAG)
main.o: main.cpp
	$(CC) $(CFLAGS) main.cpp $(INC_DIR)
clean:
	rm main.o main
